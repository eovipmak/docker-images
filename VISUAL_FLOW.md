# Monitors Management - Visual Flow Diagram

## User Journey Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                         User Access Flow                         │
└─────────────────────────────────────────────────────────────────┘

    Browser
       │
       ├── Navigate to /domains
       │
       ▼
┌──────────────────────────┐
│  Monitors List Page      │  ← domains/+page.svelte
│  ┌────────────────────┐  │
│  │  [Add Monitor]     │  │ ← Click opens modal
│  └────────────────────┘  │
│                          │
│  ┌────────────────────┐  │
│  │  Search: [_____]   │  │ ← Real-time filter
│  └────────────────────┘  │
│                          │
│  MonitorTable Component  │
│  ┌────────────────────┐  │
│  │ Name  URL  Status  │  │ ← Sortable headers
│  ├────────────────────┤  │
│  │ Site1 ⚫ Up  [Edit] │◄─┼─ Click row → Detail
│  │ Site2 ⚫ Up  [Del]  │  │   Click Edit → Modal
│  │ Site3 🔴 Down[Edit]│  │   Click Del → Confirm
│  └────────────────────┘  │
└──────────────────────────┘
       │
       ├── Click "Add Monitor" or "Edit"
       │
       ▼
┌──────────────────────────┐
│   MonitorModal           │  ← MonitorModal component
│  ┌────────────────────┐  │
│  │ Name: [_______]    │  │
│  │ URL:  [_______]    │  │
│  │ Interval: [300]    │  │
│  │ Timeout:  [30]     │  │
│  │ ☑ Enable SSL      │  │
│  │ SSL Days: [30]     │  │
│  │ ☑ Enabled         │  │
│  │                    │  │
│  │ [Cancel] [Save]    │  │
│  └────────────────────┘  │
└──────────────────────────┘
       │
       ├── Click Save
       │
       ▼
    POST/PUT /api/v1/monitors
       │
       ▼
    Success → Refresh List
    Error   → Show Error Message

       │
       ├── Click Monitor Row
       │
       ▼
┌──────────────────────────────────────┐
│  Monitor Detail Page                 │  ← domains/[id]/+page.svelte
│  ┌────────────────────────────────┐  │
│  │  [← Back] Site Name            │  │
│  │  https://example.com    ⚫ Up  │  │
│  └────────────────────────────────┘  │
│                                      │
│  ┌─────────────────────────────────┐ │
│  │ Stats Cards                     │ │
│  │ ┌────┐┌────┐┌────┐┌────┐      │ │
│  │ │Stat││Stat││Stat││Stat│      │ │
│  │ └────┘└────┘└────┘└────┘      │ │
│  └─────────────────────────────────┘ │
│                                      │
│  ┌─────────────────────────────────┐ │
│  │ Uptime Chart (24h)              │ │
│  │ ████████████████████████        │ │
│  │ Green = Up, Red = Down          │ │
│  └─────────────────────────────────┘ │
│                                      │
│  ┌─────────────────────────────────┐ │
│  │ Response Time Chart             │ │
│  │ ▁▂▃▄▅▆▇█▇▆▅▄▃▂▁                │ │
│  │ Blue bars = Response time (ms)  │ │
│  └─────────────────────────────────┘ │
│                                      │
│  ┌─────────────────────────────────┐ │
│  │ SSL Certificate (HTTPS)         │ │
│  │ Valid: Yes                      │ │
│  │ Expires: 2024-12-31            │ │
│  │ Issuer: Let's Encrypt          │ │
│  └─────────────────────────────────┘ │
│                                      │
│  ┌─────────────────────────────────┐ │
│  │ Recent Checks Table             │ │
│  │ Time     Status  Code  RT       │ │
│  │ 10:00    Up      200    45ms    │ │
│  │ 10:05    Up      200    42ms    │ │
│  │ 10:10    Down    500    -       │ │
│  └─────────────────────────────────┘ │
└──────────────────────────────────────┘
```

## Component Architecture

```
┌─────────────────────────────────────────────────────────┐
│                    Application Layer                     │
└─────────────────────────────────────────────────────────┘

SvelteKit App
├── Routes
│   ├── /domains (+page.svelte)
│   │   │
│   │   ├── Uses: MonitorTable
│   │   ├── Uses: MonitorModal
│   │   ├── State: monitors[], isLoading, error, isModalOpen
│   │   │
│   │   └── Event Handlers
│   │       ├── loadMonitors() → GET /api/v1/monitors
│   │       ├── handleAddMonitor() → Opens modal
│   │       ├── handleEditMonitor(monitor) → Opens modal with data
│   │       ├── handleDeleteMonitor(monitor) → DELETE /api/v1/monitors/:id
│   │       └── handleViewMonitor(monitor) → Navigate to detail
│   │
│   └── /domains/[id] (+page.svelte)
│       │
│       ├── Uses: MonitorStatus
│       ├── State: monitor, checks[], sslStatus
│       │
│       └── Data Loaders
│           ├── GET /api/v1/monitors/:id
│           ├── GET /api/v1/monitors/:id/checks
│           └── GET /api/v1/monitors/:id/ssl-status
│
└── Components
    ├── MonitorTable.svelte
    │   │
    │   ├── Props: monitors[]
    │   ├── State: sortField, sortDirection, searchQuery
    │   │
    │   ├── Features
    │   │   ├── Search/Filter
    │   │   ├── Column Sorting
    │   │   └── Row Actions (Edit, Delete)
    │   │
    │   └── Events
    │       ├── on:view → Emits monitor object
    │       ├── on:edit → Emits monitor object
    │       └── on:delete → Emits monitor object
    │
    ├── MonitorModal.svelte
    │   │
    │   ├── Props: isOpen, monitor (null = create)
    │   ├── State: formData, errors, isSubmitting
    │   │
    │   ├── Features
    │   │   ├── Form Validation
    │   │   ├── Create/Edit Modes
    │   │   └── Error Handling
    │   │
    │   └── Events
    │       ├── on:save → Emits saved monitor
    │       └── on:close → Emits when closed
    │
    └── MonitorStatus.svelte (existing)
        │
        ├── Props: status ('up'|'down'|'unknown'), showText
        └── Renders: Colored dot + optional text
```

## Data Flow Diagram

```
┌─────────────────────────────────────────────────────────┐
│                      Data Flow                           │
└─────────────────────────────────────────────────────────┘

┌──────────────┐
│   Browser    │
└──────┬───────┘
       │
       ├─ User Action (Create/Edit/Delete/View)
       │
       ▼
┌──────────────────────────────────┐
│  SvelteKit Page Component        │
│  ┌────────────────────────────┐  │
│  │ Event Handler              │  │
│  │ - Prepare data             │  │
│  │ - Call fetchAPI()          │  │
│  └────────────────────────────┘  │
└──────────┬───────────────────────┘
           │
           ├─ fetchAPI(endpoint, options)
           │
           ▼
┌──────────────────────────────────┐
│  API Proxy (SvelteKit Server)    │
│  /api/[...path]/+server.ts       │
│  ┌────────────────────────────┐  │
│  │ - Add auth headers         │  │
│  │ - Forward to backend       │  │
│  └────────────────────────────┘  │
└──────────┬───────────────────────┘
           │
           ├─ HTTP Request
           │
           ▼
┌──────────────────────────────────┐
│  Backend API (Go/Gin)             │
│  :8080/api/v1/*                  │
│  ┌────────────────────────────┐  │
│  │ - Verify JWT token         │  │
│  │ - Check tenant_id          │  │
│  │ - Execute business logic   │  │
│  │ - Query database           │  │
│  └────────────────────────────┘  │
└──────────┬───────────────────────┘
           │
           ├─ Database Query
           │
           ▼
┌──────────────────────────────────┐
│  PostgreSQL Database              │
│  ┌────────────────────────────┐  │
│  │ - monitors table           │  │
│  │ - monitor_checks table     │  │
│  │ - Filter by tenant_id      │  │
│  └────────────────────────────┘  │
└──────────┬───────────────────────┘
           │
           ├─ Query Results
           │
           ▼
┌──────────────────────────────────┐
│  Backend Response                 │
│  JSON: {id, name, url, ...}      │
└──────────┬───────────────────────┘
           │
           ├─ Through Proxy
           │
           ▼
┌──────────────────────────────────┐
│  SvelteKit Component              │
│  ┌────────────────────────────┐  │
│  │ - Update local state       │  │
│  │ - Trigger re-render        │  │
│  │ - Show success/error       │  │
│  └────────────────────────────┘  │
└──────────┬───────────────────────┘
           │
           ├─ DOM Update
           │
           ▼
┌──────────────────────────────────┐
│  Browser UI Updated               │
│  - Table refreshed                │
│  - Modal closed                   │
│  - Charts rendered                │
└───────────────────────────────────┘
```

## State Management Pattern

```
┌─────────────────────────────────────────────────────────┐
│               State Management Flow                      │
└─────────────────────────────────────────────────────────┘

Parent Component (domains/+page.svelte)
┌─────────────────────────────────────┐
│ State:                              │
│ ┌─────────────────────────────────┐ │
│ │ monitors: any[] = []            │ │  ← Data from API
│ │ isLoading: boolean = true       │ │  ← Loading state
│ │ error: string = ''              │ │  ← Error messages
│ │ isModalOpen: boolean = false    │ │  ← Modal visibility
│ │ selectedMonitor: any = null     │ │  ← Monitor being edited
│ └─────────────────────────────────┘ │
│                                     │
│ Props Passed Down:                  │
│ ┌──────────────┐  ┌──────────────┐ │
│ │MonitorTable  │  │MonitorModal  │ │
│ │monitors=[]   │  │isOpen=bool   │ │
│ │              │  │monitor=obj   │ │
│ └──────────────┘  └──────────────┘ │
│        │                  │         │
│        ├─ Events Up ──────┤         │
│        │                  │         │
│ ┌──────▼──────────────────▼───────┐ │
│ │ Event Handlers                  │ │
│ │ - on:view → Navigate            │ │
│ │ - on:edit → Open modal          │ │
│ │ - on:delete → Confirm & delete  │ │
│ │ - on:save → Refresh list        │ │
│ │ - on:close → Close modal        │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

## Validation Flow

```
┌─────────────────────────────────────────────────────────┐
│              Form Validation Flow                        │
└─────────────────────────────────────────────────────────┘

User Submits Form
       │
       ▼
┌──────────────────────┐
│ validateForm()       │
│ ┌──────────────────┐ │
│ │ Check each field │ │
│ └──────────────────┘ │
└──────┬───────────────┘
       │
       ├─ Name: Required & Not Empty
       ├─ URL: Required & Valid Format (new URL())
       ├─ Check Interval: >= 60 seconds
       ├─ Timeout: 5-120 seconds
       └─ SSL Alert Days: >= 1 day
       │
       ▼
┌──────────────────────┐
│ Validation Result    │
└──────┬───────────────┘
       │
       ├─ Valid → Continue
       │           │
       │           ▼
       │      POST/PUT API
       │           │
       │           ▼
       │      Success → Close Modal
       │      Error   → Show Error
       │
       └─ Invalid → Show Errors
                    │
                    ▼
               Display red text
               below each field
```

## Chart Rendering Flow

```
┌─────────────────────────────────────────────────────────┐
│              Chart Rendering Flow                        │
└─────────────────────────────────────────────────────────┘

Load Monitor Detail Page
       │
       ├─ Fetch check history
       │
       ▼
┌──────────────────────┐
│ Process Check Data   │
│ ┌──────────────────┐ │
│ │ Sort by time     │ │
│ │ Filter nulls     │ │
│ │ Limit to 48      │ │
│ └──────────────────┘ │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────────────────┐
│ Uptime Chart (CSS Bars)          │
│ ┌──────────────────────────────┐ │
│ │ For each check:              │ │
│ │   <div                       │ │
│ │     class="flex-1"           │ │
│ │     class:bg-green={success} │ │
│ │     class:bg-red={!success}  │ │
│ │     style="height: X%"       │ │
│ │   />                         │ │
│ └──────────────────────────────┘ │
└──────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│ Response Time Chart (CSS Bars)   │
│ ┌──────────────────────────────┐ │
│ │ Calculate max response time  │ │
│ │ For each check:              │ │
│ │   height = (time/max)*100%   │ │
│ │   <div                       │ │
│ │     class="flex-1 bg-blue"   │ │
│ │     style="height: X%"       │ │
│ │   />                         │ │
│ └──────────────────────────────┘ │
└──────────────────────────────────┘
       │
       ▼
   Rendered Charts
   ├─ Responsive
   ├─ Interactive (hover)
   └─ Fast (pure CSS)
```

This visual diagram provides a comprehensive overview of how all components, data flows, and user interactions work together in the Monitors Management implementation.
