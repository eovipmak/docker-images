[variables]
# NIXPACKS_NODE_VERSION = '22'
NODE_ENV = 'production'

[start]
  providers = ['node']
  cmd = './entrypoint.sh'

[phases.setup]
nixPkgs = ['caddy', 'nodejs']

[phases.build]
cmd = ['chmod +x ./entrypoint.sh']